<% if( target ){
    _.each(target, function(targetItem){ %>
        <%   if(targetItem.target_detail) { %>
            <span data-title="<%= target.target_pref_name %>" rel="<%= targetItem.metastore_id %>" class="target-preview"><%= targetItem.target_detail.value %> </span>
    <%} })
} %>

